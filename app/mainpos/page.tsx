import React from "react";
// Render the client component directly. `ClientMainPos` includes `"use client"`.
import ClientMainPos from "./ClientMainPos";

export default function MainPosPage() {
  return <ClientMainPos />;
}
